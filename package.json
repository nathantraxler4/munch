{
  "scripts": {
    "clean": "npx eslint --fix && npx prettier . --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.3.3"
  },
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*"
  ]
}
